# Prompt user for word
word = (input("Enter a word:"))
# Prompt user for times repeated
repeat = (int(input("How many times to repeat it?")))
# Prints word correct amount of times
print(word * repeat)
